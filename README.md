For linux users only:
1. Clone this repo
2. cd into ~/hack
3. run the commands in the terminal:
  chmod +x ./install-gestures.sh
  chmod +x ./run-gestures.sh
4. Run the install script. DO NOT USE FISH SHELL(only zsh or bash)
5. after installation, reboot
6. To run: . ~/hack/run-gestures.sh
